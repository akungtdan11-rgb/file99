local bots = [[

]]

for line in string.gmatch(bots, "([^\n]+)") do
    line = line:match("^%s*(.-)%s*$") 
    if line ~= "" then
        local bot = {
            ['name'] = line,
            ['platform'] = 0, --platform goes here
        }
        addBot(bot)
    end
end